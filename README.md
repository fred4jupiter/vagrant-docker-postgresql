# vagrant-docker-postgresql

Vagrant environment starting a PostgreSQL within Docker.

## Starting the vagrant box

```bash
vagrant plugin install vagrant-vbguest
vagrant plugin install vagrant-docker-compose

vagrant up
```